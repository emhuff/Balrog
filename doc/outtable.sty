\newcommand{\thickh}{\noalign{\hrule height 2.0pt}}
\newcommand{\thinh}{\noalign{\hrule height 0.02pt}}
\newcommand{\ofile}[1]{\codett{#1}}
\newcommand{\orow}[2]{ & \ofile{#1} \\ & #2}
\newcommand{\dline}[1]{\multicolumn{2}{l}{\dfill #1 \dfill}}

\newcommand{\outtab}{
\setlength{\extrarowheight}{2pt}
\vspace{10pt}
\begin{longtabu} to \textwidth {p{0.4cm} X}
\caption{File structure of the output written by \balrog{}. * is replaced with the base name of the input image} \label{tab:out}\\ \toprule \toprule
\\
%\textbf{Path} & \textbf{Contents} \\ \midrule \\

\dline{Output Images} \\
\opt{outdir}\codett{balrog\_image/}  \\ \\
\orow{*.nosim.fits}{ Copy of (subsampled) input image.}\\ \\
\orow{*.sim.fits}{Image containing simulated galaxies.}\\ \\
\orow{*.weight.sim.fits}{Copy of (subsampled) weight map.}\\ \\
\orow{*.psf}{Copy of (subsampled) \psfex{} PSF image.}\\ \\ \\

\dline{Output Catalogs} \\
\opt{outdir}\codett{/balrog\_cat/}  \\ \\
\orow{*.measuredcat.nosim.fits}{\sex{} catalog measured over original image, prior to simulation. Only written if \opt{noempty} is unflagged.} \\ \\
\orow{*.measuredcat.sim.fits}{\sex{} catalog measured over image which includes simulated galaxies.} \\ \\
\orow{*.truthcat.sim.fits}{Truth catalog of the simulated galaxies' properties.}\\ \\ \\


\dline{\sex{} Configuration Files} \\ \\
\opt{outdir}\codett{/balrog\_sexconfig/}  \\ \\
\orow{*.assoc.nosim.txt}{Association mode matching list for \sex{} run prior to adding simulated galaxies. Only written if \opt{noempty} and \opt{noassoc} are unflagged.}\\ \\
\orow{*.assoc.sim.txt}{Association mode matching list for \sex{} run with simulated galaxies. Only written if \opt{noassoc} is unflagged.}\\ \\
\orow{\$\{--sexconfig\}}{Explicit copy of file given by \opt{sexconfig}.} \\ \\
\orow{\$\{--sexparam\}}{Explicit copy of file given by \opt{sexparam}.} \\ \\
\orow{\$\{--sexemptyparam\}}{Explicit copy of file given by \opt{sexemptyparam}.} \\ \\
\orow{\$\{--sexnnw\}}{Explicit copy of file given by \opt{sexnnw}.} \\ \\
\orow{\$\{--sexconv\}}{Explicit copy of file given by \opt{sexconv}.} \\ \\

\orow{*.measuredcat.nosim.sex.config}{\sex{} \codett{config} file actually with run prior to simulation. If this file exists,
it is closely related, but not identical to \opt{sexconfig} (c.f. \autoref{sec:cmdline}). 
Only written if need be.}\\ \\

\orow{*.measuredcat.sim.sex.config}{\sex{} \codett{config} file actually used with run post simulation. If this file exists,
it is closely related, but not identical to \opt{sexconfig} (c.f. \autoref{sec:cmdline}). 
Only written if need be.}\\ \\

\orow{*.measuredcat.nosim.sex.params}{\sex{} \codett{param} file used during run prior to inserting simulated galaxies. 
Closely related, but not necessarily identical to \opt{sexparam} (c.f. \autoref{sec:cmdline}). 
Only written if \opt{noempty} is unflagged.}\\ \\

\orow{*.measuredcat.sim.sex.params}{\sex{} \codett{param} file used during run including simulated galaxies.
Closely related, but not necessarily identical to \opt{sexparam} (c.f. \autoref{sec:cmdline}).} \\ \\ \\


\dline{Log Files} \\ \\
\opt{outdir}\codett{/balrog\_log/}   \\ \\
\orow{run.log.txt}{Any \balrog{} warnings, errors, or other messages generated a run. \opt{logverbosity} applies to this file} \\ \\
\orow{args.log.txt}{Logs the command line parameters given for a \balrog{} run.} \\ \\
\orow{sex.log.txt}{\sex{} command line input/output.} \\ \\
\orow{\$\{--pyconfig\}}{Explicit copy of file given by \opt{pyconfig}} \\ \\
\bottomrule \bottomrule

\end{longtabu}
\setlength{\extrarowheight}{0pt}
}

