\newcommand{\thickh}{\noalign{\hrule height 2.0pt}}
\newcommand{\thinh}{\noalign{\hrule height 0.02pt}}
\newcommand{\ofile}[1]{\hspace{15pt} \texttt{*#1}}
\newcommand{\orow}[2]{\ofile{#1} & #2}

\newcommand{\outtab}{
\vspace{10pt}
\begin{longtabu} to \linewidth {|X X|}
\caption{File structure of the output written by \balrog{}. * is replaced with the base name of the input image} \label{tab:out}\\ \thickh
\textbf{Path} & \textbf{Contents} \\ \thickh

\texttt{--outdir/balrog\_cat/} & Output catalog files \\  
\thinh
\orow{.measuredcat.nosim.fits}{\sex{} catalog over original (subsampled) image, prior to simulation.} \\
\orow{.measuredcat.sim.fits}{\sex{} catalog over image which includes simulated galaxies.} \\
\orow{.truthcat.sim.fits}{Truth catalog of the simulated galaxies' properties.}\\
\thickh

\texttt{--outdir/balrog\_image/} & Output images\\ 
\thinh
\orow{.nosim.fits}{ Copy of (subsampled) input image.}\\
\orow{.sim.fits}{Image containing simulated galaxies.}\\
\orow{.example.psf}{Copy of (subsampled) \psfex{} PSF image.}\\
\thickh

\texttt{--outdir/balrog\_log/} & Log files\\ 
\thickh

\texttt{--outdir/balrog\_sexconfig/} & Files used to configure \sex{}\\
\thinh
\orow{.assoc.nosim.txt}{\sex{}'s association mode matching list for run without simulated galaxies.}\\
\orow{.assoc.sim.txt}{\sex{}'s association mode matching list for run with simulated galaxies.}\\
\orow{.measuredcat.nosim.sex.params}{Copy of input \sex{} parameter file used for run without simulated galaxies, which properly factors in association mode.}\\
\orow{.measuredcat.sim.sex.params}{Copy of input \sex{} parameter file used for run with simulated galaxies, which properly factors in association mode.} \\
\thickh

\end{longtabu}
}

